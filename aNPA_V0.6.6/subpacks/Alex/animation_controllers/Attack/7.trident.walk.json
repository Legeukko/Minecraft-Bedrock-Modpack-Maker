{
	"format_version" : "1.10.0",
	"animation_controllers" : {
		"controller.animation.attack.trident.a.b": {
			"states": {
				"default": {
					"blend_transition": 0.15,
					"transitions": [
						{
							"1" : "!variable.fr && variable.attack && variable.t && !query.is_sneaking && !query.is_in_water && !query.is_sprinting && !query.is_using_item"
						}
					]
				},
				"1": {
					"animations": [
						"attack.trident.a.b1",
						"-0.7"
					],
					"blend_transition": 0.15,
					"transitions": [
						{
							"default": "(query.all_animations_finished && !variable.attack) || variable.fr || !variable.t || query.is_sneaking || query.is_in_water || query.is_sprinting || query.is_using_item"
						},
						{
							"2" : "query.any_animation_finished && variable.attack && !variable.fr && variable.t && !query.is_sneaking && !query.is_in_water && !query.is_sprinting && !query.is_using_item"
						}
					]
				},
				"2": {
					"animations": [
						"attack.trident.a.b2",
						"-0.7"
					],
					"blend_transition": 0.15,
					"transitions": [
						{
							"default": "(query.all_animations_finished && !variable.attack) || variable.fr || !variable.t || query.is_sneaking || query.is_in_water || query.is_sprinting || query.is_using_item"
						},
						{
							"3" : "query.any_animation_finished && variable.attack && !variable.fr && variable.t && !query.is_sneaking && !query.is_in_water && !query.is_sprinting && !query.is_using_item"
						}
					]
				},
				"3": {
					"animations": [
						"attack.trident.a.b3",
						"-0.7"
					],
					"blend_transition": 0.15,
					"transitions": [
						{
							"default": "(query.all_animations_finished && !variable.attack) || variable.fr || !variable.t || query.is_sneaking || query.is_in_water || query.is_sprinting || query.is_using_item"
						},
						{
							"1" : "query.any_animation_finished && variable.attack && !variable.fr && variable.t && !query.is_sneaking && !query.is_in_water && !query.is_sprinting && !query.is_using_item"
						}
					]
				}
			}
		},
		"controller.animation.attack.trident.a.l": {
			"states": {
				"default": {
					"blend_transition": 0.15,
					"transitions": [
						{
							"1" : "!variable.fr && variable.attack && variable.t && !query.is_sneaking && !query.is_in_water && !query.is_sprinting && query.ground_speed<1 && !query.is_using_item"
						}
					]
				},
				"1": {
					"animations": [
						"attack.trident.a.l1",
						"-0.7"
					],
					"blend_transition": 0.15,
					"transitions": [
						{
							"default": "(query.all_animations_finished && !variable.attack) || variable.fr || !variable.t || query.is_sneaking || query.is_in_water || query.is_sprinting || query.ground_speed>1 || query.is_using_item"
						},
						{
							"2" : "query.any_animation_finished && variable.attack && !variable.fr && variable.t && !query.is_sneaking && !query.is_in_water && !query.is_sprinting && query.ground_speed<1 && !query.is_using_item"
						}
					]
				},
				"2": {
					"animations": [
						"attack.trident.a.l2",
						"-0.7"
					],
					"blend_transition": 0.15,
					"transitions": [
						{
							"default": "(query.all_animations_finished && !variable.attack) || variable.fr || !variable.t || query.is_sneaking || query.is_in_water || query.is_sprinting || query.ground_speed>1 || query.is_using_item"
						},
						{
							"3" : "query.any_animation_finished && variable.attack && !variable.fr && variable.t && !query.is_sneaking && !query.is_in_water && !query.is_sprinting && query.ground_speed<1 && !query.is_using_item"
						}
					]
				},
				"3": {
					"animations": [
						"attack.trident.a.l3",
						"-0.7"
					],
					"blend_transition": 0.15,
					"transitions": [
						{
							"default": "(query.all_animations_finished && !variable.attack) || variable.fr || !variable.t || query.is_sneaking || query.is_in_water || query.is_sprinting || query.ground_speed>1 || query.is_using_item"
						},
						{
							"1" : "query.any_animation_finished && variable.attack && !variable.fr && variable.t && !query.is_sneaking && !query.is_in_water && !query.is_sprinting && query.ground_speed<1 && !query.is_using_item"
						}
					]
				}
			}
		}
	}
}
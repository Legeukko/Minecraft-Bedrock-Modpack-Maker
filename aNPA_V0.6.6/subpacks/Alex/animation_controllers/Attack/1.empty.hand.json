{
	"format_version" : "1.10.0",
	"animation_controllers" : {
		"controller.animation.attack.empty.hand": {
			"states": {
				"default": {
					"blend_transition": 0.1,
					"transitions": [
						{
							"1" : "!variable.fr && variable.attack && !variable.is_holding_right && !variable.is_holding_left && !query.is_sneaking && query.is_on_ground && query.ground_speed < 0.5 && !query.is_in_water"
						}
					]
				},
				"1": {
					"animations": [
						"attack.empty.hand.l",
						"-0.7"
					],
					"blend_transition": 0.1,
					"transitions": [
						{
							"2": "!variable.fr && query.any_animation_finished && variable.attack && !variable.is_holding_right && !variable.is_holding_left && !query.is_sneaking && query.is_on_ground && query.ground_speed < 0.5 && !query.is_in_water"
						},
						{
							"e1": "!variable.fr && query.all_animations_finished && !variable.attack && !variable.is_holding_right && !variable.is_holding_left && !query.is_sneaking && query.is_on_ground && query.ground_speed < 0.5 && !query.is_in_water"
						},
						{
							"default": "(query.all_animations_finished && !variable.attack) || variable.is_holding_right || variable.is_holding_left || query.is_sneaking || !query.is_on_ground || query.ground_speed > 0.5 || query.is_in_water || variable.fr"
						}
					]
				},
				"2": {
					"animations": [
						"attack.empty.hand.r",
						"-0.7"
				],
				"blend_transition": 0.1,
					"transitions": [
						{
							"1": "!variable.fr && query.any_animation_finished && variable.attack && !variable.is_holding_right && !variable.is_holding_left && !query.is_sneaking && query.is_on_ground && query.ground_speed < 0.5 && !query.is_in_water"
						},
						{
							"e2": "!variable.fr && query.all_animations_finished && !variable.attack && !variable.is_holding_right && !variable.is_holding_left && !query.is_sneaking && query.is_on_ground && query.ground_speed < 0.5 && !query.is_in_water"
						},
						{
							"default": "(query.all_animations_finished && !variable.attack) || variable.is_holding_right || variable.is_holding_left || query.is_sneaking || !query.is_on_ground || query.ground_speed > 0.5 || query.is_in_water || variable.fr"
						}
					]
				},
				"e1": {
					"animations": [
						"attack.empty.hand",
						"-1.3"
				],
				"blend_transition": 0.2,
					"transitions": [
						{
							"2": "!variable.fr && query.any_animation_finished && variable.attack && !variable.is_holding_right && !variable.is_holding_left && !query.is_sneaking && query.is_on_ground && query.ground_speed < 0.5 && !query.is_in_water"
						},
						{
							"default": "(query.all_animations_finished && !variable.attack) || variable.is_holding_right || variable.is_holding_left || query.is_sneaking || !query.is_on_ground || query.ground_speed > 0.5 || query.is_in_water || variable.fr"
						}
					]
				},
				"e2": {
					"animations": [
						"attack.empty.hand",
						"-1.3"
				],
				"blend_transition": 0.2,
					"transitions": [
						{
							"1": "!variable.fr && query.any_animation_finished && variable.attack && !variable.is_holding_right && !variable.is_holding_left && !query.is_sneaking && query.is_on_ground && query.ground_speed < 0.5 && !query.is_in_water"
						},
						{
							"default": "(query.all_animations_finished && !variable.attack) || variable.is_holding_right || variable.is_holding_left || query.is_sneaking || !query.is_on_ground || query.ground_speed > 0.5 || query.is_in_water || variable.fr"
						}
					]
				}
			}
		}
	}
}
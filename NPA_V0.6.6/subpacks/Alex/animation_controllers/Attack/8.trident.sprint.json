{
	"format_version" : "1.10.0",
	"animation_controllers" : {
	    "controller.animation.attack.trident.st1": {
			"states": {
				"default": {
					"blend_transition": 0.1,
					"transitions": [
						{
							"1" : "!variable.fr && variable.attack && variable.t && !query.is_sneaking && !query.is_in_water && query.is_sprinting && query.is_on_ground && !variable.shield_off && !query.is_using_item"
						}
					]
				},
				"1": {
					"animations": [
						"attack.trident.st0",
						"-0.6"
					],
					"blend_transition": 0.1,
					"transitions": [
						{
							"default": "(query.all_animations_finished && !variable.attack) || variable.fr || !variable.t || query.is_sneaking || query.is_in_water || !query.is_sprinting || !query.is_on_ground || variable.shield_off || query.is_using_item"
						},
						{
							"2" : "query.any_animation_finished && variable.attack && !variable.fr && variable.t && !query.is_sneaking && !query.is_in_water && query.is_sprinting && query.is_on_ground && !variable.shield_off && !query.is_using_item"
						}
					]
				},
				"2": {
					"animations": [
						"attack.trident.st0",
						"-0.6"
					],
					"blend_transition": 0.1,
					"transitions": [
						{
							"default": "(query.all_animations_finished && !variable.attack) || variable.fr || !variable.t || query.is_sneaking || query.is_in_water || !query.is_sprinting || !query.is_on_ground || variable.shield_off || query.is_using_item"
						},
						{
							"1" : "query.any_animation_finished && variable.attack && !variable.fr && variable.t && !query.is_sneaking && !query.is_in_water && query.is_sprinting && query.is_on_ground && !variable.shield_off && !query.is_using_item"
						}
					]
				}
			}
		},
		"controller.animation.attack.trident.st2": {
			"states": {
				"default": {
					"blend_transition": 0.1,
					"transitions": [
						{
							"1" : "!variable.fr && variable.attack && variable.t && !query.is_sneaking && !query.is_in_water && query.is_sprinting && query.is_on_ground && !variable.shield_off && !query.is_using_item"
						}
					]
				},
				"1": {
					"animations": [
						"attack.sword.xts1",
						"-0.6"
					],
					"blend_transition": 0.1,
					"transitions": [
						{
							"default": "(query.all_animations_finished && !variable.attack) || variable.fr || !variable.t || query.is_sneaking || query.is_in_water || !query.is_sprinting || !query.is_on_ground || variable.shield_off || query.is_using_item"
						},
						{
							"2" : "query.any_animation_finished && variable.attack && !variable.fr && variable.t && !query.is_sneaking && !query.is_in_water && query.is_sprinting && query.is_on_ground && !variable.shield_off && !query.is_using_item"
						}
					]
				},
				"2": {
					"animations": [
						"attack.sword.xts1",
						"-0.6"
					],
					"blend_transition": 0.1,
					"transitions": [
						{
							"default": "(query.all_animations_finished && !variable.attack) || variable.fr || !variable.t || query.is_sneaking || query.is_in_water || !query.is_sprinting || !query.is_on_ground || variable.shield_off || query.is_using_item"
						},
						{
							"1" : "query.any_animation_finished && variable.attack && !variable.fr && variable.t && !query.is_sneaking && !query.is_in_water && query.is_sprinting && query.is_on_ground && !variable.shield_off && !query.is_using_item"
						}
					]
				}
			}
		}
	}
}